# GOX CLI

Este directorio contiene el código fuente del CLI principal de GOX Framework.

## Estructura

- `main.go` - Punto de entrada del CLI
- `commands/` - Implementación de comandos
- `config/` - Configuración del CLI